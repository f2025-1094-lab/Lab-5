#include <iostream>
using namespace std;
int main()
{
    int n;
    long long fact=1;
    cout<<"enter the numbers:";
    cin>>n;
    int i=1;
    while (i<=n){
        fact=fact*i;
        i++;

    }
    
cout<<"factorial="<<fact;
    return 0;
}
